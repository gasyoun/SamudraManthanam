# Project Objective: Samudra Manthanam Web Platform

Build a production-quality FastAPI/SQLite web search engine for the Sanskrit–Russian parallel corpus, replacing the legacy Lazarus desktop app.

---

## ➡️ Next Steps (Queue)

### H2 2026 roadmap (see `ROADMAP_2026_H2_DH_MOBILE.md`, decided 2026-06-12)
Design-session sequencing lives in `docs/DESIGN_SESSIONS_PLAN.md` (5 frontier sessions; S1 done).
- **Phase 0 cleanup remaining (mechanical, good for Haiku/Sonnet):**
  - Review `title_en`/`provenance`/`rights` across all 148 meta.json files.
  - Add `structure: verse|dictionary|prose` field to all 148 meta.json (classes defined in `docs/LINE_ID_SCHEME.md` §3).
- **S2 converter spec ✅ DONE** (`docs/CONVERTER_SPEC.md`, 2026-06-12). Four parse paths, JSONL schema with segment suffixes, commentary linkage, SLP1/accent handling, 7 CI gates. Key finding: one citation_block = Sanskrit+Russian+commentary siblings → alignment group key is free for S3; dicts already carry 4 scripts/line.
- **S3 alignment spec ✅ DONE** (`docs/ALIGNMENT_SPEC.md`, 2026-06-12). Decisive finding: alignment is markup-extraction, not inference (Sanskrit+Russian already siblings in one citation_block; 78,219 clean 1:1 blocks). n:m pre-solved by source range-merge. Edge cases bounded: 2 translation-only texts (balmont/mify-drind 100% monolingual), 10,145 monolingual-RU blocks, refrains, nav headings. Group model + per-block cardinality + gold oracle + 5 CI gates. Open for M.G.: §9.1 compare monolingual rows, §9.2 vedanga_jyotisha gap.
- **Three design specs complete (S1+S2+S3)** + **Phase 1 implementation hand-off** (`docs/IMPLEMENTATION_HANDOFF_PHASE1.md`). Phase 1 fully specified + ready to build (cheaper tier).
- **Phase 2 planned** (`docs/PHASE2_PLAN.md`): responsive 380px audit + font subset/woff2 (consolidate to one Devanagari, ~3.3MB→<200KB first-paint), hand-rolled PWA manifest+SW (no bundler), offline reader (caches rendered HTML in v1, decoupled from Phase 1). Defines the **S4 contract surface** (§3) so S4 builds on it. Parallelizable with Phase 1 (different files, no shared schema).
- **Two parallel build tracks now ready (both Sonnet-tier):** (1) Phase 1 converter/alignment per the hand-off; (2) Phase 2 PWA per PHASE2_PLAN. S4 unblocks once Phase 2 is *implemented*; S5 after S4 implementation.
- **`structure` field backfill ✅ DONE** (all 148 active sources: 119 verse / 15 dictionary / 14 prose, via `web/ingest/backfill_structure.py`, rule = CONVERTER_SPEC §9.3). Also fixed `extract_source_meta.py` to glob `*.htm` (was silently skipping 4 `.htm` sources).
- **Phase 1**: canonical JSONL data layer (explicit Sanskrit↔Russian alignment, SLP1 normalization, cross-script query expansion); HTML becomes generated view. `indic_transliteration` already a dependency.
- **Phase 2**: responsive/mobile polish + installable PWA + offline reader cache.
- **Phase 3**: full offline search via sqlite-wasm + slimmed per-text packs (≤150 MB target); golden queries vs wasm in CI.
- **Phase 4**: citation strings, JSON/CSV result exports, English metadata pages + JSON-LD, freeze desktop app.
- Decisions locked: corpus rights stay grey (no open dumps/DOI), lightweight JSONL not TEI, browser-PWA not native ports, 6-month AI-driven.

### Deferred / open
- **4 un-ingested dharmaśāstra texts (DECISION NEEDED).** `naradasmriti.html`, `vishnu-smriti.html`, `yajnavalkyasmriti.html`, `yajnavalkyasmriti_add.html` exist in `Data/` with meta.json skeletons but are NOT in `Programdata/data.txt`, so they're absent from corpus.db (148 active vs 152 on disk). Either re-add to data.txt + re-ingest (brings them into search, census, structure backfill), or confirm intentional exclusion and remove the stale meta.json. Found during structure backfill 2026-06-12.
- **M3**: `reader.py` `fetchall()` for large sources — defer until source sizes become a concern.
- **SSE endpoint**: `GET /api/search/stream` retained but untested and not wired to the frontend. Decide: keep with tests, or remove before next release.
- **Test for M1 403 path**: `test_phase3.py` covers the happy path for `/pending`; a test for missing/wrong key is still missing.
- **Corpus tests** (`pytest -m corpus`): require real corpus at `USE_REAL_CORPUS=1`. Needs a fixture strategy or a small sample corpus committed to the repo.
- **morph_cache cleanup in corpus.db**: the `morph_cache` table still exists in `corpus.db` schema (harmless dead weight). A future ingest run will recreate it empty. Can be dropped in a later schema migration.

---

## 🚧 Current Work-In-Progress (WIP)

**Phase 1 Step 1 COMPLETE** — `web/corpus_builder/html_to_canonical.py` converter implemented and all 5 measurable CI gates pass.

### Gates verified (2026-06-13):
- **Gate 1** (round-trip ID stability): PASS — identical IDs across two conversion runs
- **Gate 3** (range coverage ≥99%): PASS — 0 range misses / 0 unparseable across all 53 A-range sources
- **Gate 4** (ID uniqueness + dup suffixes): PASS — 574,939 unique IDs; 25 dup-suffix records (41 known duplicate passages, letter-suffixed correctly)
- **Gate 5** (commentary linkage): PASS — all 41,862 commentary records have annotates that resolves to an emitted verse passage
- Gates 2 (HTML round-trip renderer) and 6 (golden queries) require Phase 2/4 infra

### Converter technical notes:
- **4 parse paths**: A-clean (66 verse files, `citation_block id`), A-range (53 verse files, range-title regex), B-dict (15 dict files, 5-field tab or non-tab), C-prose (14 prose files)
- **MBh 3-level IDs**: book number prepended from slug (`06_mahabharata-bhishmaparva` → book 6); range titles include Roman numerals
- **Commentary annotates resolution** (Gate 5): two-pass — pre-scan content-bearing citation_block IDs, then resolve: exact match → pada-stripped → host passage fallback
- **Comment anchor formats handled**: simple `comment_ch_v[pada]`, 3-part `comment_ch_v_sub`, range with ASCII/en/em/figure dashes, dot-sub-index (Gitagovinda), letter-chapter (Gitarthasamgraha), broad prefix-extraction fallback
- **Prose IDs**: chapter-scoped `c{N}.p{local_seq}` when chapter headings detected; flat `p{n}` otherwise; note numbers NOT used as IDs (restart within chapter)
- **Output**: 574,939 records, 148 JSONL files in `web/corpus_builder/jsonl/`

### Next build steps (Phase 1):
- **Step 2**: Alignment groups — verify group/cardinality fields are correct, compute alignment stats vs ALIGNMENT_SPEC baselines (buddhacharita-balmont 8,852×`0:1`; mify-drind 1,172×`0:1`)
- **Step 3**: Gold fixtures — `web/tests/fixtures/alignment_gold.jsonl` (~25 hand-verified groups) + `web/tests/test_converter.py` + `web/tests/test_alignment.py`
- **Step 4**: Rebase `ingest.py` onto JSONL; add `canonical_id` column to `corpus_lines`
- **Step 5**: HTML round-trip renderer (Gate 2)

---

## 🧠 Dev Notes & Hypotheses

- `expand_word` opens two short-lived `aiosqlite` connections per call (read, then write). Acceptable for the infrequent morphological search path; if morph becomes a hot path, consolidate into a single connection.
- `_cache_read` / `_cache_write` silently swallow exceptions so a broken `state.db` never surfaces as a search error. The trade-off is that cache failures are invisible unless log output is monitored.
- The `live_search_url` in export metadata is a relative URL (`/?q=...`). If the export HTML is opened from a different origin, the link will break. Acceptable for a downloadable local file — users are expected to open it in a browser that has the app available.
- `GET /api/search/context` queries `corpus_lines` by `line_num BETWEEN ? AND ?`. If a source has non-contiguous line numbers (gaps from ingest), context may return fewer lines than `window` — correct behaviour, not a bug.
- The anchor permalink (`/sources/{id}/anchor/{link_id}`) uses a 302 redirect. Corpus link_ids observed so far are dotted integers (`1.10`) — URL-safe. If link_ids ever contain slashes or hashes, callers must percent-encode them.
- `state.db` is opened fresh per-request (no connection pool). Acceptable at current scale; watch for write serialisation under concurrent correction submissions.
- `ADMIN_SECRET_KEY` defaults to `""`. In production, if unset, `/api/admin/vacuum` and `/api/corrections/pending` are permanently inaccessible. A startup warning log when `APP_ENV == "production"` and key is empty would help operators.

---

## ✅ Completed (Recent)

- **Phase 1 Step 1: HTML→JSONL converter** (2026-06-13): `web/corpus_builder/html_to_canonical.py` — 574,939 records, 148 sources, all 5 measurable CI gates pass. Commentary anchor patterns fully covered (simple/3-part/range/dot-sub/letter-chapter/broad-fallback); Gate 5 two-pass annotates resolution; chapter-scoped prose IDs.

- **Line-ID scheme design (S1) + design-sessions plan** (2026-06-12): `docs/LINE_ID_SCHEME.md` — `{work}:{passage}` scheme, 3 source classes, duplicate disambiguation (41 known dup pairs measured), minting/immutability rules, acceptance criteria; 2 of 3 open questions resolved in-session. `docs/DESIGN_SESSIONS_PLAN.md` — S1–S5 sequencing with inputs/deliverables per session.
- **meta.json → DB wiring** (2026-06-12, commit `5b8d5f4`): 7 bibliographic columns on `sources` + ALTER TABLE migration, ingest reads sibling `.meta.json`, `/api/sources` exposes fields, reader header shows credit/year. 6 of 8 missing years recovered from source headers (2 genuinely undatable: anushasanaparva placeholder, yoga-sutry anthology). 386/386 hermetic tests.
- **Phase 0 of H2 roadmap** (v1.21.0, 2026-06-12): docs archived to `docs/archive/`; CITATION.cff + README rights section; corpus version in footer (`app/corpus_info.py`); slug-based search permalinks + canonical URLs (`_resolve_source_filter`, legacy-id fallback); 148 meta.json skeletons via `web/ingest/extract_source_meta.py`. 383/383 hermetic tests. Commits `8148beb`..`b2e18af` + changelog.
- **Source filter permalink stability** (was in Deferred): CLOSED — permalinks now carry slugs.
- **Compare route v1** (v1.13.0): `GET /compare/{work}/{ch}.{v}` for Bhagavadgītā (14 sources incl. MBh-Bhīṣma bridge), Yoga-Sūtra (4 sources), Śatakatraya (2 sources). Range-merge fallback, IAST extraction, JSON-LD, prev/next pager. 17 new tests, 107/107 hermetic pass. Smoke-verified against real corpus.db.
- **Bug sweep #10** (v1.12.10): ingest range-title fallback — Bhīṣma-parvan + Gītagovinda + 23k MBh verses now have routable `link_id`s. 10 new tests, re-ingested live corpus.db (commit `5e1c8cd`).
- **Bug sweep #8** (v1.12.9): GET search/export and search/stream now share the same 1-1000 char query constraint as POST. 92/92 hermetic tests pass.
- **Bug sweep #7** (v1.12.8): test_health_ok placeholder replaced with real assertions; lifespan now logs warning if corpus.db is missing or has zero sources. 89/89 hermetic tests pass.
- **Bug sweep #6** (v1.12.7): lifespan no longer crashes on state.db init failure (degrades), systemd hardening directives, nginx config rewritten for safe certbot bootstrap + security headers. 89/89 hermetic tests pass.
- **Bug sweep #5** (v1.12.6): ingest source_count from actual rows, morph route length cap, get_state_db wrapped in 3 more handlers, dead selection0.js removed. 88/88 hermetic tests pass.
- **Bug sweep #4** (v1.12.5): selection.js wired to main page (dead prev/next/nearest fixed), AI + corrections input bounds (abuse-vector prevention), test_phase3/4 settings.APP_ENV restored. 86/86 hermetic tests pass.
- **Bug sweep #3** (v1.12.4): CORS production fail-open (`or` → `and`), publish.py smoke_check / integrity_check connection leaks, search-before-sources-loaded UX. 82/82 hermetic tests pass.
- **Bug sweep #2** (v1.12.3): morph URL query-injection, anchor_redirect URL-encoding, health/ai error-text leaks, state-DB leak from /api/health. 80/80 hermetic tests pass.
- **Bug sweep** (v1.12.2): UTM URL builder handles `?`-bearing SS_URL; restoreFromUrl clears on no-q; state.db migration race; sitemap XML escapes `PUBLIC_BASE_URL`; identity/admin no longer leak `str(e)`; engaged CTA no longer re-animates. 77/77 hermetic tests pass.
- **Engaged-user CTA** (v1.12.1): localStorage search counter + sliding bottom banner after 3 searches; sticky dismissal; distinct `utm_medium=engaged_banner`. 73/73 hermetic tests pass.
- **Funnel foundations** (v1.12.0): SYSTEMA_SANSCRITICUM_URL cross-link banner (navbar + source view), OG/Twitter/VK link-preview meta tags, sitemap with all sources, Telegram username + UTM attribution on lead capture, social share buttons (TG/VK/WA) on results. 71/71 hermetic tests pass.
- **Search URL popstate** (v1.11.1): browser back/forward now re-runs the search via `window.addEventListener('popstate', restoreFromUrl)`.
- **Track C — No-Docker VPS Deployment** (v1.11.0): `DEPLOYMENT.md`, `deploy/samudra.service`, `deploy/samudra.nginx`, `scripts/smoke_check.py`.
- **Track A — Corpus Publication Workflow** (v1.10.0): `validate.py`, `publish.py` (6-step atomic pipeline), `reindex.sh` (no-Docker), `test_publish.py` (12 tests). 61/61 hermetic tests pass.
- **Track B cont. — morph + export** (v1.9.1): morph_cache migrated to state.db, logging fixed, graceful degradation added; export gains result count and live search link. 49/49 hermetic tests pass. — commit `3e848db`
- **Track B — Scholarly Workbench** (v1.9.0): search permalink URLs, context window API + lazy expand UI, citation copy button, anchor permalink route. — commit `3b52824`
- **Medium + nit fixes** (v1.8.1, M1, Q1–Q4): `/pending` authenticated, modulo fix, `| safe` comment, `CancelledError` propagation, audit doc corrected. — commit `2c5d792`
- **Post-review fixes** (v1.8.1, C1–C3, M2, M4): lifespan startup, `ADMIN_SECRET_KEY` decoupled, bare `except` narrowed, health DB leak fixed, test import order. — commit `a4f9486`
- **Gemini Flash Phases 1–5** (v1.8.0): state.db, health endpoint, reader, identity/corrections/AI routers, admin vacuum, AI service, test suite. — commit `a30c533`
- **Pre-Gemini audit** (B1–B5, S1–S4, A1–A7, Q1–Q6): all critical bugs and security issues fixed. — commit `a30c533`
