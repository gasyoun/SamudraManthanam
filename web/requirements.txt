fastapi==0.139.2
uvicorn[standard]==0.29.0
jinja2==3.1.6
indic-transliteration==2.3.56
httpx==0.27.0
aiosqlite==0.20.0
pydantic==2.13.1
python-multipart==0.0.31
sse-starlette==2.1.0
pydantic-settings==2.2.1
email-validator==2.1.1
respx==0.21.1
# corpus_builder — sanskritism rus_words filter + RU per-token morphology (H905).
# pymorphy3 ships the OpenCorpora dictionary (pymorphy-dicts-ru), the same
# КРС data Rubanova's 271 MB dict.opcorpora.txt held; used to catch the
# Кали→кал false-positive class and to tag every seg=ru token (lemma/POS/case/number).
pymorphy3==2.0.6
