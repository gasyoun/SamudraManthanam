fastapi==0.141.1
uvicorn[standard]==0.52.0
jinja2==3.1.6
indic-transliteration==2.3.82
httpx==0.28.1
aiosqlite==0.22.1
pydantic==2.13.4
python-multipart==0.0.32
sse-starlette==3.4.6
pydantic-settings==2.14.2
email-validator==2.3.0
respx==0.23.1
# Corpus manifest validation (Lane A). The published schema-v1.json is the one
# validator — a hand-written twin would drift from it silently.
jsonschema==4.26.0
# H1830: per-match timeout for user-supplied /api/search regex (stdlib re has none).
regex>=2026.7.19
# corpus_builder — sanskritism rus_words filter + RU per-token morphology (H905).
# pymorphy3 ships the OpenCorpora dictionary (pymorphy-dicts-ru), the same
# КРС data Rubanova's 271 MB dict.opcorpora.txt held; used to catch the
# Кали→кал false-positive class and to tag every seg=ru token (lemma/POS/case/number).
pymorphy3==2.0.6
